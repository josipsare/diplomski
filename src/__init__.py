"""Master's thesis: deep neural networks for financial anomaly detection.

Top-level package. Submodules:
    data        — load SEC EDGAR + stock data, build the panel
    features    — financial-ratio engineering and sequence tensors
    models      — anomaly-detection models (baselines + DL)
    impact      — analysis of anomaly impact on business outcomes
    evaluation  — detector comparison, case studies, figures
    utils       — shared helpers (config, logging, paths)
"""
